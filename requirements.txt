# SAR-Sense Python 依赖（直接依赖，版本对齐 rag_env_backup conda 环境 3.10.20）
# torch / torchvision 不在此处，由 Dockerfile 用 pytorch CPU index 单独安装，避免默认拉 2.5GB CUDA 轮子（本地环境是 2.12.0+cpu）

# --- Web 框架 ---
fastapi==0.112.4
uvicorn==0.47.0
python-multipart==0.0.28
aiofiles==25.1.0

# --- 数据校验 / 配置 ---
pydantic==2.13.4
pydantic-settings==2.14.1
PyYAML==6.0.3
python-dotenv==1.2.2

# --- 数据库 ---
SQLAlchemy==2.0.28
aiomysql==0.3.2
PyMySQL==1.2.0
cryptography==48.0.0

# --- 鉴权 ---
bcrypt==5.0.0
PyJWT==2.13.0

# --- LangChain 生态 ---
langchain==1.3.0
langchain-core==1.4.0
langchain-community==0.4.1
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
langchain-classic==1.0.7
langchain-protocol==0.0.15
langchain-ollama==1.1.0

# --- 向量库 / 检索 ---
chromadb==1.5.9                    # 1.x 均有 compaction，退出进程前需等待 compaction 完成，避免 HNSW 索引写半损坏
rank-bm25==0.2.2
jieba==0.42.1                    # 中文分词（BM25 预处理）

# --- 模型 / embedding ---
sentence-transformers==5.5.0
transformers==5.8.1
sentencepiece==0.2.1
onnxruntime==1.18.1

# --- 科学计算 / 图像 ---
scikit-learn==1.7.2
numpy==1.26.4
pandas==2.2.3
pillow==12.2.0
opencv-python==4.11.0.86         # 4.12+ 在 Python 3.10 下要求 NumPy 2.x，与当前 NumPy 1.26 不兼容
matplotlib==3.10.9
scipy==1.15.3
psutil==5.9.4
tqdm==4.67.3
packaging==26.0

# --- 本地魔改 Ultralytics / MBE-Net 运行依赖 ---
timm==1.0.27
einops==0.8.2
PyWavelets==1.8.0
dill==0.4.1
efficientnet-pytorch==0.7.1

# --- LLM / 搜索 API ---
dashscope==1.25.18
openai==2.42.0
tavily-python==0.7.24
requests==2.34.2

# --- 文件解析（extract_file_content 工具：PDF / docx / OCR）---
python-docx==1.2.0               # .docx 文档解析
PyMuPDF==1.27.2.3                # PDF 解析（import fitz）
pdfplumber==0.11.10              # eval_rag25 的 PDF 评测集构建
rapidocr-onnxruntime==1.4.4      # 图片 OCR（onnxruntime 推理）

# --- Redis（限流 + 分布式锁，utils/traffic_control.py）---
redis==5.0.8                     # redis.asyncio 异步客户端
