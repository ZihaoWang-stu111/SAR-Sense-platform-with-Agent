# SAR-Sense 工程审查报告

> 视角：资深 AI 工程师 / 后端架构师 / 技术面试官  
> 目标：帮助研究生应届生项目向真实企业级 AI 后端工程作品集靠拢。

## 一、整体评价

整体结论：**这个项目已经超过普通“能跑 Demo”的水平，具备比较完整的 AI 后端作品集雏形；最适合包装成“面向 SAR 遥感场景的 RAG + Agent + 目标检测一体化智能分析平台”。**

但如果按真实企业项目标准看，当前还需要补齐：

- 依赖管理
- 接口鉴权一致性
- 上传安全
- 服务层拆分
- 异步阻塞治理
- 测试体系
- RAG 可解释评估闭环
- 文档一致性

## 改造优先级总表

这张表按“对求职展示价值 / 工程风险 / 实现成本”的综合收益排序。建议从 P0 开始做，不要一上来做大重构。

### 本轮已完成改造（进度汇总）

> 经全量核查 + 多轮改造后，已完成项：

- **文档/入口**：[AGENTS.md](AGENTS.md) 全量重写与现状对齐；`app.py` 归档到 [legacy/streamlit_app.py](legacy/streamlit_app.py)
- **检测 service 拆分**：业务逻辑下沉到 [services/detection_service.py](services/detection_service.py)，router 薄化 + `run_in_threadpool`
- **YOLO 单例复用**：`detect_ships` 工具改用 `get_yolo_model()`，HTTP 与 Agent 工具共享同一份权重（N3 ✅）
- **任意文件读根除（N2 ✅）**：`extract_file_content` 从 Agent 工具列表移除；`detect_ships` 入参从 `image_path` 改为不透明 `upload_id`，LLM 无路径参数可注入
- **存储抽象层**：新增 [services/upload_store.py](services/upload_store.py)——对外只暴露 `upload_id`，真实路径不出后端；换 S3 只改该文件
- **上传安全加固（N4 ✅）**：`/api/extract-file` 加鉴权 + 10MB 大小限制 + MIME 白名单 + PIL magic bytes 校验 + 50MP 像素上限
- **解码炸弹防护（N10 ✅）**：HTTP 检测路径 + 聊天上传路径均加像素上限
- **路径泄漏消除**：响应不再返回 `temp_path`；前端消息只传 `upload_id` 不传路径；prompt rule 12/13 同步更新
- **检测端点全量加固**：`/api/detect` 加鉴权 + 大小 + MIME + magic bytes + 像素上限；detection.js 改用 `apiFetch` 带 token
- **上传安全常量收口**：大小/像素上限统一到 [upload_store.py](services/upload_store.py)，files.py / detection.py / detection_service.py 共用

> 仍未做的高优先级项：CORS 白名单（N1）、`/api/metrics` 鉴权（N9）、依赖管理（无 requirements.txt）、API 测试、`detect_ships` 工具 mkdtemp 残留（N11）。

| 优先级 | 事项 | 为什么排这里 | 建议产出 | 预计耗时 |
|---|---|---|---|---|
| P0 | 修正文档与项目入口一致性 | 面试官/协作者第一眼看文档；`AGENTS.md`、旧 Streamlit 入口过期会严重误导 | ✅ **已改**：`app.py` 移入 [legacy/streamlit_app.py](legacy/streamlit_app.py) 并加归档说明；[AGENTS.md](AGENTS.md) 全量重写与现状对齐；FastAPI 明确为唯一活跃前端 | 0.5 天 |
| P0 | 补依赖与启动说明 | 没有可复现环境会让项目像个人机器 Demo | `environment.yml` 或 `requirements.txt`；README 启动步骤；`.env.example` 使用说明 | 0.5～1 天 |
| P0 | 检测接口鉴权与前端统一 `apiFetch` | 现在检测是高成本接口，未统一鉴权会被问安全问题 | ✅ **已改**：`/api/detect` 加 `Depends(get_current_user)`；[detection.js](js/detection.js) 改用 `apiFetch`（带 token，401 跳登录）；detection.html 无 `data-public` 页面守卫已生效 | 0.5 天 |
| P0 | 上传安全最小加固 | 文件上传是后端高频安全追问点 | ✅ **已改**：`/api/extract-file` 与 `/api/detect` 两个端点均加鉴权 + 10MB 大小限制 + 扩展名/MIME 白名单 + PIL magic bytes 校验 + 50MP 像素上限；响应不返回 `temp_path`；大小/像素常量统一收口到 [upload_store.py](services/upload_store.py) | 1 天 |
| P0 | CORS 配置加固（核查新增） | `allow_origins=["*"]`+`allow_credentials=True` 让任意网站可持用户 localStorage 里的 JWT 发跨域认证请求，CSRF 式窃取会话/知识库 | 改为白名单 origin（如 `http://localhost:5000`）或关闭 `allow_credentials`；生产严格限定 | 0.5 天 |
| P0 | 收紧 `extract_file_content` 工具与 `/api/extract-file` 端点（核查新增） | 工具可任意读服务端文件（`.env`/`utils/security.py`/`config/db_conf.py`，密钥泄漏）；HTTP 端点无鉴权 + 无大小限制，匿名可触发 PDF/OCR 解析耗资源 | ✅ **已改**：`extract_file_content` 从 Agent 工具列表移除（LLM 无法再调）；`detect_ships` 入参从 `image_path` 改为不透明 `upload_id`（无路径注入面）；新增 [services/upload_store.py](services/upload_store.py) 存储抽象层（换 S3 只改该文件）；`/api/extract-file` 加鉴权 + 五道安全关卡 | 1 天 |
| P0 | `/api/metrics` 与 `/api/metrics/reset` 鉴权（核查新增） | 匿名可读运营可观测性数据（工具调用/成功率/时序）+ 任意清空指标 | GET 加 `Depends(get_current_user)`，`/reset` 加 `Depends(require_admin)` | 0.5 天 |
| P1 | Chat / Conversation schema 校验 | 当前直接读 JSON，参数边界不清晰 | `ChatStreamRequest`；`AppendMessageRequest.role` 改 Literal；content 长度限制 | 0.5～1 天 |
| P1 | API 基础测试 | 能证明鉴权、越权、参数校验不是口头说说 | pytest + auth/conversation/RBAC/detect 基础测试 10～15 个 | 1～2 天 |
| P1 | 质量工具 | 面试时能说清楚代码质量如何保证 | ruff/black/pytest 命令；可选 GitHub Actions 或本地脚本 | 0.5～1 天 |
| P1 | RAG 评测结果固化 | 这是项目最有 AI 工程含量的部分，应该变成可展示成果 | 保存 `eval_rag25` summary、ablation 表；README 写结论 | 1 天 |
| P2 | service 层拆分 | 结构会更像企业后端，但不宜在安全/测试前做 | ✅ **已改（detection）**：`DetectionService` 已拆出；`ChatService`、`KnowledgeService` 后续按需拆 | 2～3 天 |
| P2 | SSE 桥接细节优化 | 当前能跑，但可读性和断连处理一般 | 只捕获 `queue.Empty`；线程 daemon；记录异常日志；检测客户端断开 | 1 天 |
| P2 | Trace / request_id | 对 RAG/Agent 调试和面试讲解价值高 | 每次问答串联 API、检索、rerank、工具、LLM 的 trace_id | 2～3 天 |
| P2 | RAG Evidence Inspector | 让回答可解释、可溯源，是企业 RAG 常见要求 | ✅ **已有基础版**（后端带出 evidence metadata + 前端引用卡片）；可增强：结构化 evidence 字段、原文 snippet、answer/evidence 解耦 | 1～2 天 |
| P3 | Query rewrite / 多路召回 | 技术亮点强，但需要评测证明，否则容易变成堆功能 | QueryAnalyzer；多 query 召回融合；消融对比 | 3～5 天 |
| P3 | 报告生成 workflow 状态机 | 比纯 Prompt 更稳定，但改动范围较大 | 报告流程拆成确定状态：场景→数据→海况→RAG→报告 | 3～5 天 |
| P3 | 知识库任务化入库 | 更像真实企业知识库，但当前 Demo 阶段不是最急 | 入库任务 ID、状态查询、失败重试、进度展示 | 4～7 天 |
| P3 | 多租户知识库隔离 | 工程含量高，但会牵动数据模型和检索过滤 | knowledge_base/document 表；Chroma metadata filter | 5～8 天 |

### 最推荐的执行顺序

1. **先做 P0**：文档、依赖、检测鉴权、上传安全、**CORS 加固、`extract_file_content`/`/api/extract-file` 收紧、`/api/metrics` 鉴权**（核查新增 4 项 high/med 已进 P0）。  
   这几项最便宜，但最能避免面试中的低级扣分。
2. **再做 P1**：schema、API 测试、质量工具、RAG 评测结果固化。  
   这会让项目从“能讲”变成“可证明”。
3. **然后做 P2 里最有展示价值的两项**：service 层拆分（detection 已改，剩余 chat/knowledge 待拆） + RAG Evidence Inspector 增强（基础版已有，可做结构化字段 + snippet）。
   一个体现后端工程，一个体现 AI 工程。
4. **P3 选择一个做深即可**：Query rewrite、报告 workflow、任务化入库三选一，不要同时铺开。  
   应届生项目最忌“什么都想做一点，但每个都不完整”。

### 如果只剩 1 周

只做这 8 件（前 5 件为原计划，后 3 件为核查新增 high）：

1. ✅ 更新文档和入口说明；
2. 补 `environment.yml` / `requirements.txt`；
3. ✅ 检测接口鉴权 + 上传安全（`/api/extract-file` 与 `/api/detect` 均全量加固，detection.js 改 apiFetch）；
4. 加 8～10 个 API 基础测试；
5. 整理一份 RAG 消融评测结果截图或表格；
6. **CORS 改白名单**（[api/app.py:27-33](api/app.py#L27-L33)，N1）；
7. ✅ **`extract_file_content` 工具移除 + `detect_ships` 改 `upload_id` + `/api/extract-file` 全量加固**（N2/N4）；
8. **`/api/metrics` 与 `/reset` 加鉴权**（N9）。

> 上划线标记：✅ 已完成。剩余 5 项里，CORS、`/api/metrics` 鉴权是两个最小改动（各 0.5 天），优先做。

### 如果只剩 2 周

在 1 周版本基础上，再做：

1. Chat schema 校验；
2. ruff/black/pytest 命令；
3. `DetectionService` 最小拆分；
4. Evidence metadata 返回与引用展示；
5. README 中补“面试讲解版架构图/流程图”。


### 1. 当前项目的优点

项目方向是对的，而且很适合应届生求职展示。它不是单纯调用大模型接口，而是把多个工程模块组合起来了：

- **后端框架**：FastAPI 多路由结构，已经不是单文件脚本。
- **AI Agent**：基于 LangChain `create_agent`，有工具调用、动态 Prompt、流式输出、思维链步骤事件化展示。
- **RAG 系统**：包含 Chroma 向量库、BM25、混合检索、父子块、BGE reranker、评测脚本。
- **多模态能力**：YOLO SAR 舰船检测 + 文档知识问答 + 报告生成。
- **用户系统**：JWT 登录、MySQL、用户对话隔离、知识库 RBAC。
- **可观测性**：Agent 工具调用指标、成功率、时间线、MySQL 指标落库。
- **工程文档**：`CLAUDE.md` 已经记录了运行方式、架构、关键设计和已知边界。

从应届生项目角度，这已经不是“调包式 AI Demo”，而是可以包装成一个 **AI 应用后端平台 / RAG Agent 系统 / 遥感智能分析平台** 的综合项目。

### 2. 当前主要问题

主要问题不是“功能不够”，而是 **工程边界还不够清楚**。

1. **路由层承担了太多业务逻辑**  
   例如检测接口直接完成文件读取、临时文件保存、图片解析、YOLO 推理、base64 编码和响应组装。真实项目里这些应该下沉到 service 层。  
   ✅ **已改（detection 部分）**：检测业务逻辑已下沉到 [services/detection_service.py](services/detection_service.py)，[api/routers/detection.py](api/routers/detection.py) 只负责 HTTP 接入 + `run_in_threadpool` 调用。chat / knowledge 后续按同样模式拆。

2. **有些接口鉴权不一致**  
   Chat、Conversation、Knowledge 等接口有 JWT；但检测接口后端原先没有鉴权依赖，前端检测页也直接使用 `fetch`，没有走统一的 `apiFetch`。

3. **上传安全和资源限制不足**  
   图片检测接口缺少文件大小、MIME 类型、图片尺寸、解码异常、临时文件清理等控制。它还把服务端临时路径返回给前端。

4. **异步接口中仍有同步重任务需要隔离**  
   Agent 流式问答已经通过线程 + Queue 做了桥接；检测接口已改为 `run_in_threadpool`。后续文档入库、embedding、rerank 等也应保持同样思路，避免阻塞事件循环。  
   ✅ **已改（detection 部分）**：[api/routers/detection.py](api/routers/detection.py) 现在通过 `run_in_threadpool` 调用 [services/detection_service.py](services/detection_service.py) 的同步检测函数，YOLO 推理不再阻塞 FastAPI 事件循环。

5. **测试体系偏弱**  
   目前有 RAG/父子块/混合检索相关测试，但缺少 API 集成测试、鉴权测试、越权测试、上传安全测试、Agent 工具调用测试和 CI。

6. **文档存在多份且有过期信息**  
   `CLAUDE.md` 已更新得比较好；`AGENTS.md` ✅ **已重写**与现状对齐，旧 Streamlit 入口 ✅ **已归档**到 [legacy/](legacy/)。剩余文档风险基本清除。

### 3. 适合的发展方向

不要优先包装成“多 Agent 系统”，因为当前项目还不是典型多 Agent 协作架构。更准确、也更容易讲清楚的方向是：

> **SAR 遥感场景下的 RAG 增强智能分析平台**  
> 或  
> **面向 SAR 舰船检测的多模态 RAG Agent 后端系统**

核心卖点：

- YOLO 目标检测结果结构化；
- RAG 检索 SAR / 遥感 / 检测算法知识；
- Agent 根据用户意图调用检测、知识库、海况、场景数据等工具；
- FastAPI 后端提供登录、权限、会话、知识库、流式问答、指标监控；
- 通过离线评测验证 RAG 检索链路效果。

---

## 二、发现的潜在问题 / Bug

| 问题位置 | 问题描述 | 风险 | 修改建议 |
|---|---|---|---|
| `api/routers/detection.py` | 路由层承担 YOLO 推理 + 图片处理 + 响应组装等业务逻辑 | HTTP 层和业务层耦合，难以复用和测试 | ✅ **已改**：业务逻辑下沉到 [services/detection_service.py](services/detection_service.py)，router 只做 HTTP 接入 |
| `api/routers/detection.py` | 检测接口原先没有 `Depends(get_current_user)` 鉴权；前端检测页也直接 `fetch` | 未登录用户也可能调用昂贵 YOLO 推理接口，造成资源滥用 | 给 `/api/detect` 加登录鉴权；前端统一用 `apiFetch` |
| `api/routers/detection.py` | 上传图片无大小限制、MIME 校验、扩展名校验、图片尺寸限制、异常处理 | 大文件、伪造图片、PIL 解码炸弹、OOM 风险 | 增加 `MAX_UPLOAD_SIZE`、`content_type` 白名单、`Image.verify()`、像素上限 |
| `api/routers/detection.py` | 临时文件名只用 `sar_detect_{safe_name}`，可能冲突；写入后不清理 | 并发请求覆盖文件、临时目录堆积 | 使用 `NamedTemporaryFile(delete=False)` 或 UUID 文件名；推理后 finally 删除 |
| `api/routers/detection.py` | 响应中返回服务端 `temp_path` | 暴露服务器路径结构，安全和隐私上不规范 | 前端不需要该字段；删除或返回逻辑文件 ID |
| `api/routers/detection.py` | PIL 解码、YOLO 推理、图片转 base64 是同步重任务 | 阻塞事件循环 | ✅ **已改**：`run_in_threadpool` + 拆出 `services/detection_service.py` |
| `api/routers/chat.py` | Chat 请求直接 `await request.json()`，没有 Pydantic schema | 缺少字段类型、长度、空值、conversation_id 格式校验 | 新增 `ChatStreamRequest` schema，限制 `message` 长度和历史条数 |
| `api/routers/chat.py` | 有 `conversation_id` 时先写入 user message 并 commit，再启动 Agent；Agent 失败时可能只保存用户消息 | 会话状态不完整，用户看到失败但数据库已多一条 user 消息 | 保存 assistant error message 或增加 message 状态字段 |
| `api/routers/chat.py` | Agent 在线程内报错只返回 `str(e)`，没有完整日志上下文 | 定位问题困难 | 在线程异常处加 `logger.exception`，SSE 给用户泛化错误 |
| `api/routers/chat.py` | SSE 循环 `get_nowait()` 后 `except Exception: pass`，每 50ms sleep 轮询 | 吞掉非队列空异常，调试困难；轮询不如 async queue 干净 | 只捕获 `queue.Empty`；或改成 `asyncio.Queue` + `await` |
| `api/routers/chat.py` | Agent 线程非 daemon，且没有处理客户端断开后的取消 | 用户关闭页面后后端可能继续跑模型和工具 | 检测 `request.is_disconnected()`；支持 cancel；线程设 daemon 或用任务队列 |
| `schemas/conversations.py` | `AppendMessageRequest.role` 是自由字符串，`content` 允许空 | 恶意客户端可写入异常 role，甚至伪造 assistant/system 类消息 | 用 `Literal["user", "assistant"]`，限制 content 非空和长度 |
| `utils/security.py` | JWT secret 有默认硬编码 fallback | Demo 可接受，但面试时容易被问生产安全 | 强制从环境变量读取；开发环境可用 `.env.example` 明确说明 |
| `utils/security.py` | JWT payload 只含 user_id、username（外加标准 `exp` 过期声明）；后端 RBAC 用 username 判断 admin | 业务身份字段确实只有 user_id 和 username，措辞"只含"略不严谨（含标准 exp）；用户名即权限，不适合扩展角色体系 | User 表增加 `role` 字段，JWT 放 `role` 或每次查 DB 校验 |
| `model/factory.py` | 模型和 embedding 在 import 时直接初始化为全局单例 | import 即实例化 SDK client 对象（要求 env/config 就位、缺 `DASHSCOPE_API_KEY` 会让 import 直接抛错），但**不发网络请求**——真正请求在首次 invoke；措辞"触发外部依赖耦合"成立，"建立网络连接"不成立 | 改成 lazy factory / dependency provider，支持 mock |
| `utils/prompt_loader.py` | Prompt 加载是直接 open 文件，缺少缓存和结构化校验 | Prompt 缺失时启动后运行中才报错 | 启动时校验所有 prompt 路径；加载结果缓存 |
| `prompts/main_prompt.txt` | Prompt 要求输出“真实的自然语言思考过程” | 真实生产中不建议暴露模型完整隐藏推理；也可能让回答过长 | 改成“输出简短工具调用理由/执行计划”，不要要求真实思维链 |
| `prompts/main_prompt.txt` / `prompts/report_prompt.txt` | 两份 Prompt 对 `get_current_month` 返回格式描述不一致 | Agent 工具使用不稳定，报告流程可能解析错 | 统一工具契约，最好从工具 docstring 自动生成工具说明 |
| `AGENTS.md` | 仍说 Streamlit 是主前端、会话存 JSON、旧全局 `_thought_chain`；另外两处事实错误：把报告模式触发工具写成 `fetch_external_data`（实际是 `fill_context_for_report`），称知识库用 MD5 去重（实际 SHA-256） | 其它 AI 工具或协作者会被误导 | ✅ **已改**：[AGENTS.md](AGENTS.md) 全量重写，FastAPI 为唯一活跃前端、Streamlit 归档、触发工具/去重算法/存储/思维链机制全部纠正 |
| `app.py` | Streamlit 旧入口仍引用已删除的 `utils.conversation_manager` | 用户运行 `streamlit run app.py` 会失败 | ✅ **已改**：移入 [legacy/streamlit_app.py](legacy/streamlit_app.py)，归档说明见 [legacy/README.md](legacy/README.md)；`ConversationManager` 导入降级为 `None` 守卫，标记为不可运行历史代码 |
| `eval_rag25/pipelines.py` | 评测中临时修改 singleton hybrid engine 的 `k` 和 BM25 `k`；改 `engine.k` 实际**也同步改变了向量侧召回数**（`as_retriever(search_kwargs={'k': self.k})`），不只是 BM25；save/restore 非原子且未加 `engine._bm25_lock` | 如果并发评测或服务同时运行，可能影响其它请求；并发下恢复值还可能被污染 | 评测管道使用独立 retriever 实例，或给 retriever 提供 per-call k 参数 |
| `CLAUDE.md` | 文档说明无 lint/typecheck/build，但这本身也是工程短板 | 面试官会问如何保证质量 | 增加 ruff/black/mypy/pytest/CI，即使规则少也比没有好 |
| `AgentMetrics` | 全局 singleton 的平均响应时间不是 per-conversation | 并发时平均响应时间不准 | 每个 request 保存 start_time，metrics 方法传入 duration |

### 不确定、需要运行确认的点

这些通过源码和文档能判断出风险，但要完全确认需要运行服务或测试：

1. **MySQL session 生命周期是否完全符合预期**  
   需要运行 API 集成测试确认 `get_db()` 在异常时是否 rollback，在成功时是否 commit，以及 route 内手动 commit 是否与 dependency commit 重复。

2. **知识库删除是否能保证 Chroma、manifest、parent_docstore 三者一致**  
   需要重点运行知识库上传/删除流程，确认中途失败不会造成脏数据。

3. **Agent singleton 是否线程安全**  
   现在 chat SSE 每个请求会开线程调用同一个 agent singleton。需要压测两个用户同时聊天，观察工具调用上下文、Prompt 切换 flag、metrics 是否串扰。

4. **依赖环境是否可复现**  
   仓库根目录没有 `requirements.txt` 或锁文件，需要在新机器创建环境确认哪些包/版本是必需的。

---

## 三、工程结构优化建议

### 1. 当前结构总体是合理的

当前目录已经有比较清楚的分层：

- `api/`：FastAPI app、routers、dependencies、auth；
- `crud/`：数据库 CRUD；
- `models/`：SQLAlchemy ORM；
- `schemas/`：Pydantic schema；
- `rag/`：向量库、混合检索、父子块、reranker；
- `agent/`：Agent、tools、middleware、metrics；
- `eval/` / `eval_rag25/`：RAG 评测；
- `utils/`：配置、日志、安全、文件处理等。

这比很多应届生项目已经好很多。

### 2. 最大的结构问题：缺少 service 层

现在有些路由直接做了业务编排。例如：

- 检测路由直接做图片处理 + YOLO 推理 + 响应组装；
- Chat 路由直接处理消息写库、会话构造、Agent 线程、SSE；
- Knowledge 路由大概率直接操作 vector store；
- RAG 服务里同时做检索、重排、总结；
- vector store 里可能同时负责加载、分块、embedding、Chroma 写入、BM25 缓存。

建议增加明确的 `services/` 层：

```text
services/
  chat_service.py          # 会话消息写入、构造上下文、保存 assistant 回复
  detection_service.py     # 图片校验、YOLO 推理、结果结构化
  knowledge_service.py     # 上传、去重、入库、删除、manifest 维护
  auth_service.py          # 注册、登录、角色判断
  metrics_service.py       # 指标查询、聚合、重置
```

路由层应该只负责：

1. 参数校验；
2. 调用 service；
3. 返回响应；
4. 注入依赖和鉴权。

不要在 router 里写复杂业务逻辑。

### 3. RAG 模块建议拆得更工程化

当前 `rag/` 已经有核心模块，但可以更清晰：

```text
rag/
  ingestion/
    loaders.py             # txt/pdf 加载
    chunkers.py            # semantic / parent-child chunk
    manifest.py            # 文件去重、版本、删除
  retrieval/
    vector_retriever.py
    bm25_retriever.py
    hybrid_retriever.py
    parent_child.py
  rerank/
    bge_reranker.py
  generation/
    rag_answer_service.py
  evaluation/
    datasets.py
    metrics.py
```

面试时可以讲：

> 我把 RAG 拆成 ingestion、retrieval、rerank、generation、evaluation 五个阶段，每个阶段可替换，可做 ablation，对应真实企业知识库系统的处理链路。

### 4. 配置模块建议升级

现在配置是 YAML + import-time 全局读取。建议改为：

```text
core/
  config.py       # Pydantic Settings
  logging.py
  security.py
  exceptions.py
```

用 Pydantic Settings 统一读取：

- MySQL DSN；
- DashScope API key；
- Tavily API key；
- JWT secret；
- Chroma 路径；
- 模型名；
- 上传大小限制；
- rerank threshold。

这样简历上可以写：

> 使用 Pydantic Settings 实现多环境配置管理，支持开发/测试/生产配置隔离。

### 5. 旧入口和文档要处理 ✅ 已改

旧 Streamlit 入口已归档、AGENTS.md 已重写。具体：

- ✅ `app.py` 移入 [legacy/streamlit_app.py](legacy/streamlit_app.py)，归档说明见 [legacy/README.md](legacy/README.md)。
- ✅ [AGENTS.md](AGENTS.md) 全量重写与现状对齐：FastAPI 为唯一活跃前端、Streamlit 标记为 archived 不可运行、纠正了触发工具名（`fill_context_for_report`）、去重算法（SHA-256）、会话存储（MySQL）、思维链机制（`on_step` 回调无全局状态）等过期内容。
- ✅ 顶部加 Note 指向 CLAUDE.md 为权威参考，避免双份文档再次漂移。

---

## 四、功能增强建议

### 必须做，优先级最高

这些是最能提升工程化观感、也最容易被面试官认可的。

#### 1. 补依赖管理和启动脚本

当前没有根目录 `requirements.txt`。建议新增：

- `requirements.txt` 或 `environment.yml`；
- `.env.example` 已有，可以继续保留；
- `README.md` 或更新 `CLAUDE.md` 为普通用户可读的运行文档；
- `scripts/dev_server.bat` / `scripts/run_tests.bat`。

最低产出：

```bash
conda env export --from-history > environment.yml
pip freeze > requirements-lock.txt
```

#### 2. 给关键接口补鉴权和上传安全

重点修：

- `/api/detect` 加 `Depends(get_current_user)`；
- 检测页改用 `apiFetch`；
- 限制上传文件大小、类型、图片尺寸；
- 删除响应中的 `temp_path`；
- 临时文件用 UUID 并清理。

#### 3. 增加 Pydantic 请求 schema

尤其是：

- `ChatStreamRequest`
- `DetectionResponse`
- `AppendMessageRequest.role`
- `KnowledgeUploadResponse`
- 统一响应模型

例如：

```python
class ChatStreamRequest(BaseModel):
    message: str = Field(min_length=1, max_length=8000)
    display_message: str | None = None
    conversation_id: str | None = None
    messages: list[ChatMessage] = Field(default_factory=list, max_length=20)
```

#### 4. 增加 API 测试

建议至少补：

- 注册 / 登录 / me；
- 未登录访问 conversations 返回 401；
- A 用户不能读 B 用户会话；
- 非 admin 不能上传/删除知识库；
- `/api/detect` 未登录不能调用；
- chat stream 空消息返回 400；
- 上传非法文件返回 400。

#### 5. 清理过期文档 ✅ 已改

- ✅ [AGENTS.md](AGENTS.md) 全量重写与现状对齐；
- ✅ `app.py` 移入 [legacy/streamlit_app.py](legacy/streamlit_app.py) + [legacy/README.md](legacy/README.md) 归档说明；
- ✅ 文档明确 FastAPI 为唯一活跃前端，Streamlit 标记不可运行。

### 建议做，中优先级

#### 1. 增加 service 层

重构：

- `DetectionService`
- `ChatService`
- `KnowledgeService`
- `RagAnswerService`

这会显著提升项目“真实后端工程”的观感。

#### 2. 给耗时任务加线程池/后台任务

- YOLO 推理；
- BGE rerank；
- 文档 embedding；
- 知识库上传入库；
- Agent streaming。

Demo 阶段可以先用 `run_in_threadpool()`，不用一开始就上 Celery。

#### 3. 统一响应格式

现在成功是 `{success: True, ...}`，失败是 `{detail: ...}`。这能跑，但企业项目一般会统一：

```json
{
  "code": 0,
  "message": "ok",
  "data": {}
}
```

或者继续用 FastAPI 默认错误格式，但要统一成功响应。

#### 4. 加 trace_id 和结构化日志

每个请求生成 `request_id`，贯穿：

- API 请求；
- Agent 工具调用；
- RAG 检索；
- LLM 调用；
- DB 写入；
- SSE 输出。

这对面试很加分，因为可以讲“我不只是实现功能，还能观测系统行为”。

#### 5. RAG 评测结果接入可视化页面

你已经有 `eval_rag25/` 了。可以把结果展示到 metrics 页面：

- Recall@3/5/10；
- MRR；
- 平均检索耗时；
- 不同 pipeline 对比；
- 每个问题的命中文档和 snippet。

这会让项目从“有 RAG”升级为“有 RAG 质量评估体系”。

### 有时间再做

#### 1. 知识库多租户隔离

当前知识库是全局共享。可以增加：

- `knowledge_base` 表；
- `document` 表；
- `user_id` / `kb_id`；
- Chroma metadata filter；
- 不同用户或项目隔离文档。

#### 2. 报告生成工作流图

当前报告生成靠 Prompt + dynamic prompt。可以改成更确定的 workflow：

```text
intent classify
  -> scene_id extraction
  -> detection data
  -> sea state
  -> RAG evidence
  -> report compose
  -> citation check
```

用 LangGraph 或自己写简单状态机都可以。

#### 3. 前端工程化

当前是 vanilla JS，可以继续保留。如果时间多，可以换成 React/Vue。  
但不建议优先做前端框架迁移，因为对 AI 后端求职价值不如 RAG/Agent/后端工程化。

---

## 五、创新点建议

### 创新点 1：RAG 评测闭环与可视化看板

| 项目 | 内容 |
|---|---|
| 功能描述 | 把 `eval_rag25/` 的 Recall、MRR、平均延迟、命中文档、错误样例接入可观测性页面 |
| 实现方式 | 评测脚本输出 JSON/CSV；后端提供 `/api/eval/summary`；前端 metrics 页面展示 pipeline 对比图和错误 case |
| 难度 | 中 |
| 简历价值 | 非常高。说明你不是只会“搭 RAG”，还知道如何评估 RAG |
| 面试讲法 | “我构建了 25/50 题离线评测集，对 vector-only、BM25、parent-child、rerank、full pipeline 做消融实验，用 Recall@k 和 MRR 衡量检索质量，并把结果接入监控页面。” |

### 创新点 2：可解释 RAG Evidence Inspector ✅ 已有，可增强

> 现状：后端 [rag/rag_service.py](rag/rag_service.py) `rag_summarize` 已把 `filename / chunk_id / parent_index / match_child_id / page / rerank_score` 拼进 **context 段**（6 字段全有）；但”参考来源”sources 段**只暴露 3 字段**（filename/chunk_id/score），`parent_index/match_child_id/page` 被丢弃。前端 [js/chat.js](js/chat.js) 已有 `renderWithCitations` / `renderSingleRagCitation` / `renderMultipleRagCitations`，支持引用角标、来源卡片、多组 RAG 来源、点击交互（多 RAG 路径的角标/点击高亮有缺陷，见第八节）。**基础版已经做完，下面是可选增强项，不是必须。**

| 项目 | 内容 |
|---|---|
| 已有能力 | filename、chunk_id、parent_index、match_child_id、page、rerank_score 全部带出；前端渲染引用角标 + 来源卡片；支持多次 RAG 调用分组 |
| 可增强 1：结构化 evidence | 现在靠字符串拼接 + 前端正则解析“参考来源：”文本协议，LLM 偶尔会破坏。改为返回结构化 `evidence: [{filename, chunk_id, page, rerank_score, snippet, ...}]` 字段，前端不再正则解析 |
| 可增强 2：展示原文 snippet | 来源卡片现在只显示 `chunk / score`，没有命中原文片段。加 `doc.page_content[:200]` 作为 snippet，点开引用可看命中原文（更接近 ChatGPT / Perplexity 体验） |
| 可增强 3：解耦 answer 与 evidence | 现在 `rag_summarize` 靠 `answer.split("参考来源：")` 截断 LLM 输出。改为 `{"answer": ..., "evidence": [...]}` 分离返回，不依赖文本协议 |
| 难度 | 低～中 |
| 简历价值 | 高。企业很重视 RAG 可解释性和可追溯性，而且你**已经做了基础版**，面试可以直接演示 |
| 面试讲法 | “我没有只把检索内容塞给模型，而是保留了 evidence metadata（文件名、chunk_id、父块、rerank 分数、页码），前端渲染成可点击的引用卡片，让回答可溯源，方便定位幻觉和检索失败。后续可增强为结构化 evidence 字段 + 原文 snippet 展示。” |

### 创新点 3：Query Rewrite + 多路召回路由

| 项目 | 内容 |
|---|---|
| 功能描述 | 根据用户问题类型自动改写 query，并选择向量、BM25、关键词、文档标题、场景数据等不同召回路径 |
| 实现方式 | 增加 `QueryAnalyzer`：识别算法名、论文名、场景 ID、时间、检测指标；生成 1～3 个 query；多路召回后融合 |
| 难度 | 中到偏高 |
| 简历价值 | 高。能体现你理解 RAG 不是简单 top-k |
| 面试讲法 | “SAR 领域有大量英文缩写和算法名，单 query 容易漏召回，所以我做了 query rewrite 和多路召回融合，并通过评测集验证效果。” |

### 创新点 4：Agent 报告生成状态机

| 项目 | 内容 |
|---|---|
| 功能描述 | 把“生成 SAR 检测报告”从纯 Prompt 驱动改成确定性流程 |
| 实现方式 | 使用 LangGraph 或自定义 workflow：场景识别 → 数据查询 → 海况查询 → RAG 证据 → 报告生成 → 校验 |
| 难度 | 中 |
| 简历价值 | 很高。比“让模型自己决定”更工程化 |
| 面试讲法 | “我把开放式 Agent 和确定性 workflow 结合：普通问答走 ReAct，报告生成走状态机，降低工具乱调和流程不稳定。” |

### 创新点 5：YOLO 检测结果结构化 + RAG 专家分析 ✅ 已有，可增强

> 现状：Agent 工具 [agent/tools/agent_tools.py:428-508](agent/tools/agent_tools.py#L428-L508) `detect_ships` 已经把 YOLO 原始输出转成结构化文本返回给 LLM：目标数量、各目标置信度+bbox、平均/最小/最大置信度、**检测质量分级（高/中/偏低）**、**低置信度目标计数（<0.6）**、低置信度预警 + "建议结合海况数据和专业知识进一步分析"。这套结构化上下文让 LLM 能据置信度判断是否需要追加 RAG/海况工具调用。**基础版已实现，下面是可选增强项，不是必须。**

| 项目 | 内容 |
|---|---|
| 已有能力 | 检测框 bbox、逐目标置信度、目标数量、平均/最小/最大置信度、质量分级（高/中/偏低）、低置信度计数与预警、各目标逐条详情，全部以结构化文本进入 Agent 上下文 |
| 可增强 1：自动触发 RAG 专家分析 | 现在工具只在预警文本里**建议**"结合海况数据进一步分析"，但**没有自动触发** RAG 检索。可在低置信度/漏检时让 Agent 自动调 `rag_summarize` 查"YOLO SAR 漏检原因/海况影响/SAR 检测优化"，把检索结果作为诊断意见写进回答——这依赖 Prompt 引导，不是工具本身 |
| 可增强 2：结果可视化闭环 | [agent_tools.py:487-492](agent/tools/agent_tools.py#L487-L492) 把画好框的图存到 `mkdtemp` 临时目录 + 全局 `_last_viz_path`，但 FastAPI 前端没人消费该路径（只有 legacy `app.py` 用），"结果图"实际没传给用户。需把可视化图转 base64 或走静态文件路由返回前端 |
| 难度 | 低～中 |
| 简历价值 | 高。体现多模态 + RAG 结合，而且**已经做了基础版**，面试可直接演示 |
| 面试讲法 | "检测模型只输出框是不够的，我把 YOLO 结果转成结构化上下文（置信度分级、低置信度预警、逐目标详情）给 Agent，让它判断要不要追加知识库检索做诊断分析。后续可增强为低置信度自动触发 RAG + 结果图回传前端。" |

### 创新点 6：知识库 ingestion 任务化与版本管理

| 项目 | 内容 |
|---|---|
| 功能描述 | 上传文档后返回任务 ID，前端显示解析、分块、embedding、写库进度；支持失败重试和删除回滚 |
| 实现方式 | 简化版可以用 FastAPI BackgroundTasks + DB 表记录状态；进阶版用 Celery/RQ |
| 难度 | 中到偏高 |
| 简历价值 | 高。非常像真实企业知识库项目 |
| 面试讲法 | “文档入库是长任务，我没有阻塞 HTTP 请求，而是设计了任务状态表和进度查询接口，保证入库过程可观测、可重试。” |

### 创新点 7：Agent / RAG 全链路 Trace

| 项目 | 内容 |
|---|---|
| 功能描述 | 每次问答记录 query、召回文档、rerank 分数、工具调用、LLM 耗时、最终回答 |
| 实现方式 | 增加 `trace_id`，每个阶段写入 `trace_events` 表或 JSONL；metrics 页面展示时间线 |
| 难度 | 中 |
| 简历价值 | 很高。体现后端可观测性和 AI 系统调试能力 |
| 面试讲法 | “RAG/Agent 问题难定位，所以我做了 trace，把一次回答拆成检索、重排、工具、LLM、保存五类事件，可用于调试慢请求和幻觉问题。” |

### 创新点 8：权限与审计日志

| 项目 | 内容 |
|---|---|
| 功能描述 | 对知识库上传、删除、登录、检测、报告生成等关键操作记录审计日志 |
| 实现方式 | 新增 `audit_logs` 表；封装 `AuditService`；管理员页面可查看 |
| 难度 | 低到中 |
| 简历价值 | 中高。适合后端工程化展示 |
| 面试讲法 | “我把 AI 平台当成企业系统设计，关键操作都有审计记录，便于追踪误删、越权、资源滥用。” |

---

## 六、适合写进简历的项目描述

### 项目名称

**SAR-Sense：面向 SAR 舰船检测的多模态 RAG Agent 智能分析平台**

### 项目背景

面向 SAR 遥感舰船检测场景，构建一个集 **目标检测、知识库问答、检测报告生成、对话记忆、用户权限、可观测性和 RAG 评估** 于一体的 AI 后端系统。项目目标不是单纯演示大模型能力，而是模拟企业级 AI 应用的完整工程链路。

### 技术栈

- **后端**：Python、FastAPI、SQLAlchemy 2.0 Async、aiomysql、Pydantic、JWT、bcrypt
- **AI / Agent**：LangChain、ReAct Agent、Tool Calling、Streaming SSE、Dynamic Prompt
- **RAG**：ChromaDB、DashScope Embeddings、BM25、Parent-Child Retrieval、BGE Reranker、RAG Evaluation
- **模型服务**：ChatTongyi / DashScope、BGE reranker、YOLO / Ultralytics
- **存储**：MySQL、ChromaDB、JSON manifest、parent docstore
- **前端**：HTML/CSS/Vanilla JS、SSE 流式渲染
- **工程化**：配置管理、日志、全局异常处理、RBAC、指标监控、评测脚本

### 核心功能

1. **SAR 图像舰船检测**  
   上传 SAR 图像，调用本地 YOLO 模型完成舰船目标检测，返回检测框、置信度、目标数量和结果图。

2. **RAG 知识库问答**  
   支持 TXT/PDF 文档入库，使用向量检索 + BM25 混合召回 + BGE rerank + 父子块回表，提升专业知识问答效果。

3. **Agent 工具调用与报告生成**  
   基于 ReAct Agent 自动识别用户意图，调用 RAG、海况查询、场景数据、目标检测等工具，生成 SAR 检测分析报告。

4. **用户系统与会话管理**  
   实现 JWT 登录注册、MySQL 会话持久化、用户对话隔离、管理员知识库权限控制。

5. **可观测性与评估体系**  
   记录 Agent 工具调用、成功率、LLM 调用、对话轮次；构建 RAG 消融评测集，对不同检索 pipeline 进行 Recall@k、MRR 和延迟对比。

### 技术亮点

- 设计并实现 **混合 RAG 检索链路**：Chroma 向量召回 + BM25 关键词召回 + BGE rerank + parent-child 上下文扩展。
- 将 RAG 检索顺序优化为 **child rerank → parent resolve**，在保证效果接近的情况下减少大文本重排开销。
- 基于 LangChain middleware 实现 **工具调用监控与动态 Prompt 切换**，报告生成场景可自动切换系统 Prompt。
- 使用 SSE 实现 **Agent 流式输出与工具调用过程可视化**，通过 request-local callback 替代全局状态，降低并发串扰风险。
- 将 SQLite/JSON 原型迁移到 **MySQL + SQLAlchemy Async + CRUD 分层架构**，实现用户、会话、消息和指标持久化。
- 构建 **RAG 离线评测集与消融实验脚本**，覆盖 vector-only、hybrid、parent-child、rerank、full pipeline 等多种配置。

### 我的职责

> 独立负责项目从原型到工程化改造的设计与实现，包括 FastAPI 后端架构、JWT 鉴权、MySQL 持久化、RAG 检索链路、LangChain Agent 工具调用、YOLO 检测集成、SSE 流式问答、指标监控和 RAG 评估体系搭建。

### 可量化成果

当前可以比较稳妥地写：

- 支持 **TXT/PDF 知识库入库**，构建 SAR 检测领域知识问答系统。
- 设计 **5～6 组 RAG 消融 pipeline**，使用 Recall@k、MRR 和平均延迟评估检索效果。
- 通过 **child rerank → parent resolve** 的 RAG 顺序优化，相比旧的 parent 后 rerank 方案降低重排文本长度和推理延迟。
- 实现 **JWT + RBAC + 用户会话隔离**，支持多用户对话管理。
- 实现 **Agent 工具调用指标采集**，包括调用次数、成功率、时间线和对话轮次。

如果后续补充测试和评测结果，可以把“约 65% 延迟降低”“Recall@3 提升多少”等写得更具体。

### 当前最值得写进简历的亮点

1. **RAG 检索链路完整**：不是简单向量库，而是 hybrid + parent-child + rerank + eval。
2. **Agent 工具调用工程化**：middleware 监控、动态 Prompt、SSE 流式展示。
3. **AI + 后端结合**：有用户、权限、数据库、会话、异常处理、指标。
4. **多模态场景**：SAR 图像检测 + 文档知识问答 + 报告生成。
5. **评测意识**：有离线评测和消融实验，这是很多应届生项目没有的。

### 还缺哪些能打动面试官的功能

优先补这几个：

1. **API 测试 + RAG 评测报告**  
   面试官很喜欢问“你怎么证明效果变好了”。

2. **服务层重构**  
   展示你知道 router、service、repository 的边界。

3. **上传安全和鉴权一致性**  
   展示你有安全意识，不只是能跑。

4. **Trace / Evidence 可视化**  
   展示你能调试 RAG 和 Agent，而不是黑盒调用。

5. **知识库任务化入库**  
   展示你理解长任务、状态机、失败重试。

### 容易被面试官追问的问题

需要提前准备这些解释：

1. **为什么要 BM25 + 向量混合检索？**  
   向量适合语义相似，BM25 适合专业术语、英文缩写、型号、算法名精确匹配；SAR 领域很多缩写和论文名，混合检索更稳。

2. **为什么要 parent-child retrieval？**  
   子块适合精确召回，父块提供完整上下文；避免 chunk 太小导致回答缺上下文，也避免 chunk 太大导致召回不准。

3. **为什么先 rerank child 再 resolve parent？**  
   reranker 对短文本更准确、更快；先对子块排序，再映射到父块，可以兼顾精度和上下文完整性。

4. **RAG 怎么评估？**  
   构建 QA 数据集，用 gold filename/snippet 判断命中，计算 Recall@k、MRR、平均延迟，再做 pipeline 消融。

5. **Agent 工具乱调用怎么办？**  
   Prompt 约束只是第一层，更好的做法是意图分类 + workflow 状态机 + 工具调用上限 + trace 监控。

6. **你的系统哪里不是生产级？**  
   不要回避。可以说：目前是求职作品集级别，下一步会补依赖锁定、CI、接口测试、上传安全、任务队列、配置中心和多租户知识库。

7. **前端已经有登录守卫，为什么后端还要鉴权？**  
   这道题考的是"信任边界"——前端守卫是 UX，不是安全边界。要点：
   - 前端登录页 / `auth.js` 检查 token 只在**浏览器里生效**，攻击者可以根本不打开浏览器：`curl -X POST http://你的域名/api/detect -F "image=@x.png"` 不经过前端、不带 token，后端没 `Depends(get_current_user)` 就会照常处理。
   - 所以前端守卫挡得住"正常人走错路"，挡不住"绕过浏览器的脚本"（爬虫、curl、批量请求）。原来 `/api/extract-file` / `/api/detect` 无鉴权时，匿名脚本能狂刷 YOLO/PDF 解析把服务器打挂——前端登录页一点没拦住。
   - 安全原则：**永远不信任客户端**。前端做的检查都是"方便好人"，攻击者能完全控制浏览器、改 JS、用 DevTools。只有后端强制校验 JWT 才是真门。
   - 类比：前端登录页是"请先取号"指示牌，后端鉴权是"收银台必须出示会员卡"——指示牌挡不住直接走到收银台的人，收银台查卡才是真限制。
   - 引申：同理 CORS、上传大小/MIME 校验、路径白名单都必须**后端做**，前端校验只是体验优化，可被绕过。

---

## 七、下一步 2～4 周改造路线

### 第 1 周：工程基础加固

#### 目标

让项目从“能跑”变成“像真实后端项目”。

#### 任务

1. 补依赖和启动文档：
   - 新增 `requirements.txt` 或 `environment.yml`；
   - 新增普通用户可读的 `README.md`；
   - 明确 FastAPI 是主入口，Streamlit 是 legacy。

2. 清理过期文档 ✅ 已改：
   - ✅ [AGENTS.md](AGENTS.md) 全量重写与现状对齐；
   - ✅ `app.py` 移入 [legacy/streamlit_app.py](legacy/streamlit_app.py) + [legacy/README.md](legacy/README.md) 归档说明。

3. 接口安全：
   - `/api/detect` 加 JWT；
   - 检测前端改用 `apiFetch`；
   - 上传大小、类型、图片尺寸校验；
   - 删除响应中的 `temp_path`。

4. Schema 校验：
   - 新增 `ChatStreamRequest`；
   - `AppendMessageRequest.role` 改为 Literal；
   - content 加长度限制。

#### 产出

- 可复现启动文档；
- 安全加固后的上传接口；
- 更规范的请求 schema；
- 一份“工程化改造说明”。

### 第 2 周：测试与质量体系

#### 目标

让项目有“可证明的质量”。

#### 任务

1. 引入 pytest：
   - 配置 `pytest.ini`；
   - 把现有直接运行测试改成 pytest 风格。

2. 增加 API 测试：
   - auth；
   - conversation；
   - RBAC；
   - detect 未登录；
   - knowledge 非 admin 禁止上传；
   - chat 空消息。

3. 增加 lint/format：
   - ruff；
   - black；
   - 可选 mypy。

4. RAG smoke test：
   - 保留 `eval_rag25 --limit 2` 作为快速回归命令。

#### 产出

- `pytest` 一键测试；
- 至少 10～15 个后端测试；
- 代码格式化工具；
- 面试可讲的质量保障体系。

### 第 3 周：架构重构与可观测性

#### 目标

让代码结构更像企业项目。

#### 任务

1. 新增 service 层：
   - `DetectionService`；
   - `ChatService`；
   - `KnowledgeService`；
   - `RagService` 进一步拆分。

2. 耗时任务治理：
   - YOLO 推理使用 `run_in_threadpool()`；
   - 文档入库改为 background task；
   - SSE 检测客户端断开。

3. 增加 trace_id：
   - API request；
   - Agent tool；
   - RAG retrieval；
   - LLM call；
   - DB save。

4. Metrics 页面增加 trace 或 RAG 结果展示。

#### 产出

- 更清晰的 router-service-crud 分层；
- 可观测 trace；
- 慢请求和工具调用可追踪。

### 第 4 周：RAG / Agent 创新增强

#### 目标

让项目从“工程完整”变成“有技术亮点”。

#### 任务

1. Evidence Inspector 增强（基础版已有）：
   - ✅ 已有：引用角标、来源卡片、filename/chunk_id/parent/score/page、多组来源、点击交互；
   - 可增强：返回结构化 `evidence` 字段（不再靠“参考来源：”文本协议）；来源卡片加原文 snippet；answer 与 evidence 解耦。

2. Query rewrite / 多路召回：
   - 对算法名、英文缩写、场景 ID 做 query analyzer；
   - 支持多 query 召回融合；
   - 做 eval 对比。

3. 报告生成 workflow：
   - 把报告生成从纯 Prompt 改成状态机；
   - 限制工具调用顺序；
   - 每步有 trace。

4. 评测可视化：
   - 把 `eval_rag25/results/summary.csv` 展示到页面；
   - 支持 pipeline 对比。

#### 产出

- RAG 可解释引用；
- Query rewrite 消融结果；
- 报告生成状态机；
- 一页可截图放简历/作品集的 RAG 评测看板。

---

## 八、全量核查补遗（报告遗漏项）

> 本节是对前文一～七节的二次全量核查补遗。用 14 个并行核查 agent 对照当前代码逐条验证了前文所有断言，结论：**前文断言无一为 false/outdated**（`✅已改` 标注全部属实、遗留问题全部仍在），但有 5 处措辞不严谨（已在前文就地修正：D2 JWT payload 含 exp、E2 model factory 不发网络请求、M4 parent_docstore 是 threading.Lock 非文件锁、J1 sources 段只暴露 3/6 字段、AGENTS.md 还有两处事实错误），另外发现一批**前文完全没提到的新问题**，按严重度列于下。`file:line` 均为当前代码。

### 🔴 High（4 项，前文完全未提）

| # | 位置 | 问题 | 风险 | 修复 |
|---|---|---|---|---|
| N1 | [api/app.py:27-33](api/app.py#L27-L33) | CORS `allow_origins=["*"]` + `allow_credentials=True` | Starlette 在此组合下**回显 Origin 并带 `Access-Control-Allow-Credentials: true`**（非发字面 `*`），任意网站可持用户 localStorage 里的 JWT 发跨域认证请求，CSRF 式读取会话/知识库 | 改白名单 origin（如 `http://localhost:5000`）或关 `allow_credentials`；生产严格限定 |
| N2 | [agent/tools/agent_tools.py:510-573](agent/tools/agent_tools.py#L510-L573) | `extract_file_content(file_path)` 工具无路径白名单 | LLM 选路径，可读 `.env`/`config/db_conf.py`/`utils/security.py`，把 `TAVILY_API_KEY`/DB 密码/JWT secret 灌进上下文回流给用户；RAG 文档或用户消息里的 prompt injection 即可触发 | ✅ **已改**：`extract_file_content` 已从 Agent 工具列表移除；`detect_ships` 入参从 `image_path` 改为不透明 `upload_id`，工具内部按 id 在受控目录解析（`get_upload_path`），LLM 根本没有路径参数可注入——比白名单更彻底 |
| N3 | [agent/tools/agent_tools.py:428-506](agent/tools/agent_tools.py#L428-L506) | `detect_ships` 工具每次 `YOLO('...best.pt')` 新建对象 | 与 HTTP 检测接口（[services/detection_service.py](services/detection_service.py) 用 `get_yolo_model()` 单例）是两套并行加载路径，Agent 工具这条从未接单例：N 次调用 = N 份权重常驻内存 + 重复多秒冷启动；相对路径还依赖 CWD | ✅ **已改**：[agent_tools.py:438](agent/tools/agent_tools.py#L438) 改用 `get_yolo_model()` 复用单例，HTTP 与 Agent 工具现在共享同一份已加载 YOLO 权重；相对路径问题随单例内部 `get_abs_path` 一并解决 |
| N4 | [api/routers/files.py:11-12](api/routers/files.py#L11-L12) | `/api/extract-file` 无鉴权 + 无大小限制 | 匿名可上传任意文件触发 PyMuPDF/docx/RapidOCR 解析（CPU/内存耗尽），响应还返回服务端 `temp_path` | ✅ **已改**：端点加 `Depends(get_current_user)` 鉴权；10MB 大小限制（413）；图片走 MIME 白名单 + PIL magic bytes 校验 + 50MP 像素上限；响应改为返回不透明 `upload_id`（不泄漏服务端路径）；新增 [services/upload_store.py](services/upload_store.py) 存储抽象层，换 S3 只改这一个文件 |

### 🟡 Med（9 项）

| # | 位置 | 问题 | 风险 |
|---|---|---|---|
| N5 | [utils/exception_handlers.py:20-31](utils/exception_handlers.py#L20-L31) + [crud/conversations.py:85-95](crud/conversations.py#L85-L95) | IntegrityError 处理器把任何 duplicate 映射成"用户名已存在"；`next_idx=max+1` 非原子 | 会话 `message_index` UNIQUE 冲突时，用户发消息却收到"用户名已存在"；并发 append 会撞 UNIQUE |
| N6 | [api/routers/chat.py:80-81](api/routers/chat.py#L80-L81) | agent 线程无 join/无取消 | 客户端断连后线程继续跑 LLM/工具调用直到 ReAct 自行结束，event_queue 无界堆积 |
| N7 | [api/routers/chat.py:51](api/routers/chat.py#L51) | 无 conv_id 分支直接信任 `messages_history` | 可注入 `role:'system'` 做 prompt injection，绕过 `report_prompt_switch`/`dynamic_prompt` |
| N8 | [config/db_conf.py:29-37](config/db_conf.py#L29-L37) + [api/routers/chat.py:130](api/routers/chat.py#L130) | `get_db` yield 依赖在 StreamingResponse 完成才 close | DB AsyncSession 整个 SSE 期间不释放，~30 并发流式聊天耗尽连接池（pool_size=10+max_overflow=20），拖垮所有路由 |
| N9 | [api/routers/metrics.py:11-12,28-29](api/routers/metrics.py#L11-L12) | `/api/metrics` 与 `/api/metrics/reset` 无鉴权 | 匿名可读运营指标 + 任意清空 |
| N10 | [services/detection_service.py:25](services/detection_service.py#L25) | 无 `Image.MAX_IMAGE_PIXELS`/像素上限 | PIL 解码炸弹：小 PNG 解压成 gigapixel → OOM。✅ **已改**：HTTP 检测路径 + 聊天上传路径均加 50MP 像素上限 + PIL `verify()` magic bytes 校验 |
| N11 | [agent/tools/agent_tools.py:485-490](agent/tools/agent_tools.py#L485-L490) | `detect_ships` 工具 `mkdtemp` 不清理 + `_last_viz_path` 全局变量 | 临时目录永不删 → 磁盘堆积；并发 detect_ships 互相覆盖 viz 路径 |
| N12 | [utils/conversation_builder.py:69,87](utils/conversation_builder.py#L69) | async 函数里调 `chat_model.invoke`（同步网络请求） | 冻住整个事件循环数秒，期间所有并发请求停滞；应用 `await ainvoke` 或 `run_in_threadpool` |
| N13 | [eval_rag25/pipelines.py:55-66](eval_rag25/pipelines.py#L55-L66) | save/restore `engine.k` 非原子且未加 `engine._bm25_lock`；改 `engine.k` 也改了向量召回数 | 并发下恢复值可能被污染；评测窗口期生产请求读到被改写的 k（已在前文 H1 行补充） |

### 🟢 Low（7 项）

| # | 位置 | 问题 |
|---|---|---|
| N14 | [agent/tools/agent_tools.py:424,506,573](agent/tools/agent_tools.py#L424) | 工具返回 `f'失败：{str(e)}'` 给 LLM → 内部细节泄漏（路径/Tavily 401/ultralytics 栈），用户可据此探测路径存在性 |
| N15 | [schemas/conversations.py:11](schemas/conversations.py#L11) | `AppendMessageRequest.role` 裸 str → 可持久化 `role:'system'` 消息做**存储型** prompt injection（每次加载该会话都触发） |
| N16 | [agent/metrics_collector.py:27,31](agent/metrics_collector.py#L27) | `tool_call_records`/`tool_durations` 无界 append → 长时间运行内存泄漏 |
| N17 | [utils/prompt_loader.py:13,26,39](utils/prompt_loader.py#L13) + [agent/tools/middleware.py:39-44](agent/tools/middleware.py#L39-L44) | prompt_loader 无缓存，`@dynamic_prompt` 每个模型步重读磁盘；一次报告生成触发 7+ 次磁盘 I/O |
| N18 | [api/app.py:72-73](api/app.py#L72-L73) | startup preload 把 prompt 加载失败降级为 warning，服务"健康"启动直到首个 chat 才 500；无健康检查暴露该降级状态 |
| N19 | [js/chat.js:1255-1268](js/chat.js#L1255-L1268) | 多 RAG 场景 `renderMultipleRagCitations`：`decorateCitationBody` 未把 `[n]` 替换为角标、`.citation-source-item` 缺 `data-idx` → 点击高亮永远失效 |
| N20 | [rag/parent_docstore.py:13](rag/parent_docstore.py#L13) | 仅 `threading.Lock`（进程内），无跨进程文件锁；多 worker（gunicorn/uvicorn --workers N）部署并发写 `parent_docstore.json` 会丢数据或损坏 |

### 核查方法说明

本轮用 14 个并行核查 agent 分区域对照当前代码逐条验证前文断言（detection / chat / schemas / security / model / prompts / AGENTS.md / eval / metrics / evidence / knowledge / deps / rag-concurrency / 新问题扫描），对每条断言判 confirmed/outdated/partial/false，对"报告已过期或报告错了"的断言再走一轮对抗性 re-check（默认怀疑核查员结论、倾向报告其实对）。结论：前文无 false/outdated 断言，5 处 partial 已就地修正，上表 N1～N20 为核查新发现、已并入优先级总表（N1/N2/N4/N9 进 P0）。

---

## 最后定位建议

这个项目不要包装成“我做了一个 ChatGPT 套壳”。更建议这样定位：

> 我做的是一个面向 SAR 遥感检测场景的 AI 后端系统，核心挑战不是调用大模型，而是把目标检测、RAG 检索、Agent 工具调用、用户系统、权限控制、会话持久化、评测和可观测性组合成一个可维护的工程项目。

如果完成第 1～2 周内容，这个项目已经可以作为不错的应届生 AI 后端作品集。  
如果再完成第 3～4 周，尤其是 **RAG 评测可视化、Evidence Inspector、报告 workflow、Trace 监控**，它会明显高于普通应届生项目，面试时也更有内容可讲。
